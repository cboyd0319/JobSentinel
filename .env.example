# JobSentinel v1.0 Environment Variables (Optional)
#
# These environment variables are OPTIONAL. Configuration is primarily done through
# config.json, but you can use these for overrides or CI/CD scenarios.

# Slack Webhook URL (overrides config.json if set)
# Get this from: https://api.slack.com/messaging/webhooks
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Database URL (optional - defaults to %LOCALAPPDATA%\JobSentinel\jobs.db)
# DATABASE_URL=sqlite:///C:/Users/YourName/AppData/Local/JobSentinel/jobs.db

# Logging level (trace, debug, info, warn, error)
# RUST_LOG=info

# JobsWithGPT MCP endpoint (optional - uses default if not set)
# JOBSWITHGPT_ENDPOINT=https://api.jobswithgpt.com/mcp

# Scraping interval override (in hours)
# SCRAPING_INTERVAL_HOURS=2

# Disable automatic scheduling (manual trigger only)
# DISABLE_AUTO_SCRAPING=false
