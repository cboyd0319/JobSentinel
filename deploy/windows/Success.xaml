<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Setup Complete" Height="300" Width="500"
        WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True" Background="Transparent">
    <Border Background="#F9FAFB" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="10">
        <Grid Margin="20">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="✅ All Done!" FontSize="24" FontWeight="Bold" Foreground="#10B981" HorizontalAlignment="Center"/>
                <TextBlock Text="Job Finder has been successfully installed." FontSize="16" Foreground="#1F2937" HorizontalAlignment="Center" Margin="0,10,0,10"/>
                <ListView x:Name="HealthCheckList" Margin="0,10,0,10" BorderThickness="1" BorderBrush="#E5E7EB">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Check" DisplayMemberBinding="{Binding Name}" Width="200"/>
                            <GridViewColumn Header="Status" Width="200">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Status}" FontWeight="Bold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="Pass">
                                                            <Setter Property="Foreground" Value="#10B981"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Fail">
                                                            <Setter Property="Foreground" Value="#EF4444"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
                <Button x:Name="LaunchButton" Content="Launch Job Finder" FontSize="18" FontWeight="Bold" Padding="15,10" MinHeight="50" Background="#4C8BF5" Foreground="#FFFFFF" BorderThickness="0" Margin="0,10,0,0"/>
                <Button x:Name="CloseButton" Content="Close" FontSize="16" Margin="0,10,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
