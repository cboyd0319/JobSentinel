# v2.9.0 Completion And Full-Feature Roadmap

Last updated: 2026-06-19.

## Problem

JobSentinel needs one clear source of truth for what remains before the
`2.9.0` release and what remains before the product can reasonably be called
full featured.

The release and product goals are related but not the same, but this active
goal does not allow release action until both are reconciled:

- `2.9.0` is close to release-complete. The remaining work is final gate
  execution, release publication, asset verification, and truthful signoff.
- Full-featured JobSentinel still has roadmap work after `2.9.0`: richer
  user-controlled browser assistance, broader source coverage, stronger resume
  and application workflows, deeper job-quality analysis, and lower-friction
  signed distribution.
- For this active goal, do not push, retag, publish, or otherwise cut a release
  until the backlog completion checklist below is closed or the user explicitly
  changes the release scope.

## Product Direction

JobSentinel's product direction for this goal is maximum job-search help with
minimum user friction. When ordinary implementation guidance conflicts with
making the product easier, more automatic, or more comprehensive for a job
seeker, prefer the path that helps the user find and manage jobs.

Use these defaults:

- Make the easy path automatic when the action is local, reversible, and
  user-reviewed.
- Remove arbitrary roadblocks, redundant confirmations, engineer-only setup,
  and manual copy/paste work.
- Prefer warning, consent, review, and a secure local path over blocking when
  the user is intentionally using a source they control.
- Keep broad public and unauthenticated sources low-friction.
- Keep authenticated or restricted-source work user-initiated, visible, and
  local by default.

The hard stops remain privacy and security failures, credential or session
capture, hidden background access, unsafe destinations, data loss, platform
control bypass, unbounded source access, and external side effects the user did
not choose.

## Scope And Non-Goals

In scope:

- Final `2.9.0` pre-release verification and release operations.
- Remaining source, Browser Import, and restricted-source validation.
- Product capabilities still needed for a mature, full-featured job-search
  assistant.
- Platform, security, privacy, documentation, and user-ease gaps that affect
  release confidence.
- Exact commands and evidence needed before claiming release completion.

Non-goals:

- Capturing or storing cookies, tokens, browser storage, authorization headers,
  or session material.
- Silent background access for authenticated restricted sources.
- Autonomous LinkedIn scraping, scheduled authenticated monitoring, or
  platform-control bypass.
- Claiming Gatekeeper-ready macOS distribution before signing, notarization,
  stapling, and install proof exist.
- Treating every future product idea as a `2.9.0` blocker.

## Constraints And Risks

- Rule 0 controls every milestone: user data stays local unless the user
  explicitly configures an external channel, external AI is optional and
  disabled by default, and sensitive actions require explicit user review.
- Windows 11+, macOS, and Linux behavior must remain intact. If a platform
  cannot be live-checked, the gap must be named.
- Package managers, direct npm packages, npm overrides, Cargo crates, GitHub
  Actions, and build tools must stay exact-pinned to current latest stable where
  upstream constraints allow it.
- Public unauthenticated sources should stay low-friction. Restricted public
  boards can require acknowledgement, rate limits, and clear warnings, but not
  authenticated-session time caps when no sign-in is required.
- Authenticated restricted sources require a plain-language warning and
  explicit user action. JobSentinel can provide the safest local path, but must
  not persist sign-in material or run hidden refreshes.
- Browser Import may import visible, user-chosen data through a user action. It
  must not read hidden page state, browser storage, network traffic, screenshots,
  or authorization material.
- External release blockers are real: Apple Developer ID signing, Windows code
  signing and SmartScreen reputation, source-specific API keys or partner terms,
  and GitHub Actions availability cannot be solved only with local code.

## What Remains For v2.9.0

### 1. Freeze Feature Work

Before release operations start, do not begin another broad feature slice unless
it fixes a failing gate, user-data risk, security issue, source blocker,
documentation drift, or installer problem.

Expected result:

- `main` has a clean local worktree except intentional release metadata.
- The active status, current work plan, release risk register, source-debug
  ledger, verification matrix, README, wiki, and release notes describe the
  same behavior.

### 2. Run Final Local Gates From The Latest Commit

Run the final release gate from the verified commit, currently expected to be
the latest intentional commit after the script layout, documentation, screenshot,
scraper, and resume-verification slices land.

```bash
npm run harness:check
npm run release:readiness -- --version 2.9.0
npm run release:check-deps
npm run doctor
npm run lint:docs
npm run lint:bloat
npm run lint:security
npm run lint
npm run test:run
npm run test:e2e:all
npm run build
cd src-tauri && cargo fmt --all -- --check
cd src-tauri && cargo clippy -- -D warnings
cd src-tauri && cargo test --lib
```

Expected result:

- All checks pass from the release commit.
- Any failure is fixed, documented, rechecked, and committed before tagging.

### 3. Recheck Browser Import And LinkedIn-Compatible Paths

Run the focused checks for the newest visible-card import behavior and the
user-controlled LinkedIn-compatible Workbench flow.

```bash
npm run test:run -- src/components/BrowserImportButtonScript.test.ts src/components/LinkedInWorkbench.test.tsx src/components/BookmarkletGenerator.test.tsx
cd src-tauri && cargo test --lib bookmarklet
```

Final release signoff must retain evidence that:

- Restricted-source warning copy is plain, friendly, and accurate.
- Browser Import captures only visible user-selected or visible-page job data.
- Imported records remain reviewable before durable use.
- URLs are scrubbed of query strings, fragments, and token-like values before
  storage where required.
- No cookies, storage state, auth headers, HAR, trace, video, or session files
  are produced by authenticated-source testing.

### 4. Confirm Manual UI Coverage

The goal is not complete until every route, click, action, modal, toast, empty
state, loading state, error state, settings panel, import flow, keyboard path,
and narrow-width surface has been exercised and recorded.

Minimum evidence:

- Route inventory screenshots or equivalent Playwright/Computer Use evidence.
- Whole-UI click and keyboard pass.
- Source setup, restricted-source acknowledgement, Browser Import, Workbench,
  reminders, settings, setup wizard, application tracking, resume assistance,
  alerts, data management, privacy, and support surfaces.
- No console errors, broken focus paths, overflow, passive keychain prompts, or
  copy that assumes engineering knowledge.

### 5. Refresh README, Docs, And Screenshots

The main `README.md`, docs hub, release notes, and public screenshots must match
the actual `2.9.0` product surface before release operations resume.

Minimum evidence:

- New screenshots for the current dashboard, job tracking, source/import,
  resume, settings, and privacy/security surfaces.
- README copy that clearly explains the `2.9.0` product, local-first privacy,
  supported source models, Browser Import/Workbench behavior, skills downloads,
  no-account release posture, and platform expectations.
- Stale docs are either updated, moved to an archive with clear status, or
  deleted when they no longer describe a shipped or planned surface.
  Historical release/archive records may remain if clearly archival.

### 6. Manually Verify Scraper And Resume Surfaces

Automated tests are not enough for this goal. Every current personal scraper,
public source adapter, restricted-source compatible flow, and resume feature
must receive manual verification evidence before release signoff.

Minimum evidence:

- Every configured current scraper/source contract is exercised with a fixture,
  low-volume live probe, or documented source-specific no-account limitation.
- Restricted or authenticated sources are checked through the approved
  user-controlled flow and produce no stored credentials, cookies, auth headers,
  browser storage, traces, HAR files, screenshots with private data, or session
  artifacts.
- Resume parsing, importing, matching, tailoring, builder/export, ATS/readability
  review, application-form assistance, cover-letter review, and screening-answer
  review are tested against the local resume corpus and documented references.

### 7. Push And Publish Release Assets

Release sequencing:

0. Confirm the backlog completion checklist in this plan is closed.
1. Wait for any older `v2.9.0` workflow run to finish.
2. Push `main` and the public wiki after final local gates pass.
3. Move `v2.9.0` to the verified commit.
4. Let the hosted release workflow build platform assets.
5. Verify the published release, checksums, SBOMs, attestations, skills
   archives, installer names, and install instructions.

Expected result:

- Windows no-account artifacts are published as unsigned where appropriate and
  clearly labeled.
- macOS no-account universal DMG is published with accurate Gatekeeper language.
- Linux AppImage and deb artifacts are published with checksums and SBOMs.
- Skills tar.gz and ZIP archives are published for download.
- The release page, README, docs, wiki, and verification records match.

## Known v2.9.0 External Gaps

These do not need to block a no-account `2.9.0` release if they are documented
truthfully:

- Apple Developer ID signing, notarization, stapling, and Gatekeeper acceptance.
- Windows code-signing certificate and SmartScreen reputation.
- Any source-specific partner API credentials not already configured locally.
- Public hosted workflow failures caused by external service outage rather than
  repo logic.

## Backlog Completion Checklist

This checklist is the authoritative backlog for the active goal. Release work is
paused until every non-external row is either complete with evidence or explicitly
rescoped by the user.

### User-Controlled Browser Assistance

JobSentinel should become more helpful during human-controlled browser sessions
without becoming a hidden scraper.

| Requirement | State | Evidence or next action |
| ----------- | ----- | ----------------------- |
| Visible controls for applied, tracking, save, note, and visible import | Complete locally | Workbench exposes one-click applied, save, tracking, note, and related local ledger actions; Browser Import now exposes visible job-card imports as Jobs waiting for review with Save Job, Skip, Save All, Skip All, and Refresh controls. Focused restricted-source UI tests pass. |
| Ledger entries for applied, saved, rejected, interview, follow-up, reminder, notes, and not interested | Complete locally | `LinkedInWorkbench` and mock/native event paths cover expanded event types. Keep tests current. |
| Interest learning from saved, dismissed, search, notes, imports, and feedback | Complete locally | Opt-in local learning now records only explicit user actions from Workbench, Browser Import saves, job bookmarks, dismissed jobs, saved notes, saved searches, and Useful/Not for me feedback. Signals store action, title/company, or saved-search name only; no notes, URLs, browser state, cookies, or hidden page data. |
| Watch-and-learn on/off state with exact privacy copy | Complete locally | Workbench learning starts off, explains recorded button/title/company signals and excluded browser/session data, and includes clear learning. |
| No durable record without user confirmation | Complete locally | Workbench records only explicit user-clicked local actions. Browser Import now queues sanitized visible-page imports in memory and writes durable jobs only after Save Job or Save All confirmation; focused frontend and Rust bookmarklet tests pass. |

### Source Expansion And Discovery

JobSentinel should help users find jobs beyond the stock boards.

| Requirement | State | Evidence or next action |
| ----------- | ----- | ----------------------- |
| Reviewed definitions or adapters for Greenhouse, Lever, Ashby, SmartRecruiters, Workday, Workable, Recruitee, Breezy, JazzHR, Bullhorn, company pages, and regional boards | Complete locally | Shared source taxonomy has contract tests for every named ATS family, reviewed definition or adapter status, employer-career fallback, and regional-board coverage. |
| Source classification for public API, public HTML, restricted public, authenticated session, and unknown review-required sources | Complete locally | `src/shared/jobSourceDiscoveryTaxonomy` and restricted-source records distinguish access models. |
| Employer career-page discovery for companies outside broad boards | Complete locally | Employer-careers taxonomy covers user-named company career pages and fallback paths for direct company sites that do not appear on stock boards. Local API research added Workday CXS, Phenom widget, and Radancy/TalentBrew candidate lanes to the shared taxonomy with tenant-specific policy and fixture requirements. |
| Regional and country-specific coverage for United States, United Kingdom, India, Canada, and requested markets | Complete locally | Source taxonomy contract tests cover US, UK, India, and Canada discovery entries, plus regional board categories for local markets. |
| Source-debug fixtures, low-volume live probes, and restricted-domain reasons | Complete locally | 2026-06-19 refresh recorded in `docs/harness/source-debug-verification-v2.9.0.md`: scraper parser/transport contracts `580/585` with 5 network-only ignored, scraper integration `25/25`, pipeline integration `12/12`, API contract `33/33`, scheduler integration `18/19` with 1 existing ignored SQLite-specific test, source-health contracts `74/74`, Browser Import helper `47/47`, LinkedIn boundary `35/35`, restricted-source frontend tests `106/106`, and live public source probes `14/14`. |

### Resume, Matching, And Application Assistance

Resume assistance should be useful for technical and non-technical users.

| Requirement | State | Evidence or next action |
| ----------- | ----- | ----------------------- |
| Hard-requirement checks with evidence caps and fail-fast guidance | Complete locally | Rust analyzer tests cover hard caps, credential, degree, screening, experience, work arrangement, and role-family evidence; focused UI/mock/application tests cover must-have ordering, plain guidance, and hard screening review. 2026-06-19 evidence: Rust analyzer `238/238`, frontend/mock/application `181/181`. |
| ATS and readability review that never invents unsupported experience | Complete locally | Rust format-safety tests cover readable text, hidden text, prompt-injection-like content, keyword stuffing, filler, table/layout risks, and unclear capability claims; bullet and skills guidance keep edits evidence-bounded. Open resume tool/template and ATS references now feed `src/shared/resumeWritingTaxonomy.ts`, Resume Match bullet structures, Resume Builder export checks, major Greenhouse/Workday/Taleo-or-Oracle portal checks, and retail/service keyword coverage without copying third-party templates. 2026-06-19 evidence: Rust analyzer `238/238`, frontend/mock/application `181/181`, focused resume-writing UI/taxonomy `31/31`. |
| Role-family coverage for technical, content, operations, healthcare, service, trades, education, sales, and early career | Complete locally | `src/shared/resumeRoleFamilyTaxonomy.ts` defines the supported families, `ResumeRoleFamilyCoverageCard` shows them in Resume Match, and focused tests prove every family has examples plus mappings to current career profiles and analyzer evidence prompts. |
| Local semantic matching model governance and Qwen3 architecture | Complete locally | `models.lock.toml` now pins Qwen3 embedding, Qwen3 reranker, and the MiniLM baseline with revisions, hashes, sizes, licenses, backend compatibility, instruction profiles, thresholds, and stale-vector rules. Runtime compatibility, vector provenance, evidence labels, hard negatives, feedback events, blockers, training phases, seed eval fixtures, the text-only Qwen3 embedding backend, bounded Qwen3 reranker backend, deterministic hybrid scorer, and research addendum domain contracts are typed and checked in Rust. 2026-06-19 focused live validation downloaded verified Qwen3 embedding artifacts and produced a normalized 768-dimensional embedding; reranker live validation downloaded verified Qwen3 reranker artifacts and ranked direct Kubernetes security evidence above a vocabulary-overlap near miss. Direct semantic matcher command paths now prefer Qwen3 dense retrieval plus bounded Qwen3 reranking when both governed models are downloaded and verified, with MiniLM retained as a fallback. Hybrid tests prove direct evidence beats keyword-only near misses, hard blockers cap scores, and retrieval provenance records dense, BM25, exact skill, required-coverage, and reranker sources. `embedded-ml` resume/job scoring now uses the hybrid scorer with a legacy fallback when local ML is disabled. Settings now includes **Local Match Check**, backed by `get_semantic_matching_diagnostics`, to show local-only mode, Qwen3 model-lock metadata in `embedded-ml` builds, cache readiness, scoring signals, and quality checks without loading model weights or exposing resume/job text. The semantic matching research notes lock in ConFit-style hard-negative evals, Recall@K, nDCG, requirement-level evidence labels, career-path/seniority signals, role-family expansion, skill-graph/confusable-skill modeling, fairness and self-preference evals, adversarial job-posting checks, and separate real-job versus generated-advice result types. The seed eval fixture and contract tests now cover those addendum categories with focused `embedded-ml` tests. |
| Manual resume-corpus verification for every resume surface | Complete locally | 2026-06-19 private aggregate probe parsed 12 local files across DOCX, Markdown, and PDF with 0 parse failures; ran skill extraction, ATS/readability review against three broad job descriptions, bullet-review checks, and synthetic export checks without printing or storing private resume content. Focused public/synthetic frontend, E2E, semantic, and desktop/mobile manual-style passes covered UI-level import, match review, tailoring guidance, builder/export, application-form assistance, cover-letter review, screening-answer review, and Local Match Check diagnostics. |
| Form-review provenance for exact questions, confirmed answers, voluntary/protected answers, and unknowns | Complete locally | `src/shared/applicationFormReviewTaxonomy.ts` defines the four provenance categories, `ApplicationFormReviewChecklist` shows them in Application Preview, and focused tests verify exact-question, confirmed-answer, voluntary/protected, and unknown guidance. |
| Safer attachment, cover letter, and screening-answer workflows | Complete locally | Application Preview keeps resume attachment and final submit user-controlled, `ApplicationFormReviewChecklist` covers screening-answer review, `CoverLetterReviewChecklist` covers blanks/claims/job wording, and focused Application Assist plus cover-letter tests passed. |

### Job Quality And Ghost-Job Review

JobSentinel should help users spend time on better opportunities.

| Requirement | State | Evidence or next action |
| ----------- | ----- | ----------------------- |
| Duplicate and repost clustering across sources | Complete locally | Database duplicate groups cluster same title/company across sources, choose a primary by score, hide merged duplicates, ignore hidden jobs, and track repost counts separately. 2026-06-19 evidence: duplicate DB `11/11`, frontend/source guidance `124/124`. |
| Public employer or ATS follow-through checks where allowed | Complete locally | Job Import performs user-initiated public HTTPS page preview with no cookies or auth, blocks redirects/private/plaintext targets, dedupes canonical links, and keeps restricted-source acknowledgement before direct checks. Job cards open saved posting links through safe handlers for user verification. 2026-06-19 evidence: public import `19/19`, import command safety `9/9`, frontend/source guidance `124/124`. |
| Freshness, low-detail, vague-role, stale, scam-cue, and pay-risk signals with confidence | Complete locally | Ghost detection covers stale, repost, low-detail, vague-title, missing-detail, company-behavior, score, and confidence signals; JobCard keeps scam and pay cues separate from posting-risk guidance. 2026-06-19 evidence: Rust ghost `18/18`, frontend/source guidance `124/124`. |
| User feedback loops that improve scoring without claiming employer intent | Complete locally | Job cards now provide Useful/Not for me local feedback that adjusts the visible fit estimate, stores no URL/session data, and states that the adjustment does not predict employer intent; focused tests cover the shared store and UI. |
| Clear distinction among poor fit, low confidence, scam evidence, and likely ghost indicators | Complete locally | JobCard, GhostIndicator, source guidance, docs, and `job-posting-risk-review` skills separate local fit, source confidence, scam evidence, missing pay, stale/repost signals, and likely ghost indicators without claiming employer intent. 2026-06-19 evidence: Rust ghost `18/18`, frontend/source guidance `124/124`. |

### Guided Search Operations

JobSentinel should guide the job search week by week.

| Requirement | State | Evidence or next action |
| ----------- | ----- | ----------------------- |
| Intake keeps broad defaults and does not silently narrow the user | Complete locally | Setup Wizard and guided intake tests cover broad defaults; keep reviewed. |
| Weekly review, stop rules, stale thresholds, and replan-from-tracker flows | Complete locally | Applications Search review now includes **Replan this week**, routes to the summary surface, and documents stop-rule review from tracker evidence; skills include the weekly-review rubric and status-transition thresholds. 2026-06-19 evidence: guided-search tests `62/62`, `lint:skills`, `release:skills`. |
| Reviewable and explainable next-best-action suggestions | Complete locally | Applications Search review builds priority actions from reminders, quiet roles, interviews, offers, saved roles, and weekly review, with plain descriptions and visible **After this** handoff cues. 2026-06-19 evidence: guided-search tests `62/62`, `lint:skills`. |
| Referral, recruiter, outreach, follow-up, interview-prep, and offer-review handoffs in skills and UI | Complete locally | Application review actions now surface UI handoffs for networking outreach, application tracking, interview prep, offer/pay review, posting review, and job-search planning; downloadable skills keep the same cross-skill handoffs. 2026-06-19 evidence: guided-search tests `62/62`, `lint:skills`, `release:skills`. |

### Pay, Offer, And Risk Review

JobSentinel should protect users from weak pay information and rushed offers.

| Requirement | State | Evidence or next action |
| ----------- | ----- | ----------------------- |
| Written-vs-verbal offer separation | Complete locally | Pay Protection now separates written offer status, verbal/recruiter numbers, and written offer amount before drafting notes; negotiation notes stay disabled unless written terms are selected and a written amount is entered. 2026-06-19 evidence: salary UI tests `19/19`, Rust salary tests `164/164`. |
| Total compensation, commute/relocation, deadline pressure, counter/decline templates | Complete locally | Pay Protection now adds offer decision review fields for deadline, total compensation, commute, relocation, and deadline pressure, plus local counter and decline starter templates that do not submit anything. 2026-06-19 evidence: salary UI tests `19/19`, Rust salary tests `164/164`. |
| Pay transparency support by region with current law/data | Complete locally | Shared taxonomy covers current official-source-reviewed regional pay-range rules and JobCard shows a plain review cue when a covered-region saved job has no complete pay range. |
| Plain review of missing, min-only, max-only, malformed, broad, or anchoring-prone pay | Complete locally | Job cards and shared guidance cover missing, min-only, max-only, malformed, broad, below-floor, regional missing-pay, and anchoring-prone pay-floor cases with plain review copy and focused tests. |

### Distribution And Support Polish

The app should be easy for non-technical users to install, run, update, and
recover.

| Requirement | State | Evidence or next action |
| ----------- | ----- | ----------------------- |
| Signed Windows and macOS installers when credentials exist | External | No-account release path exists; signing remains credential-gated and is not required for this user's current release because Apple Developer credentials are unavailable. Do not claim signed readiness. |
| Auto-update path with integrity checks and rollback guidance | Complete locally | `2.9.0` uses an explicit no-auto-update path: Tauri updater is not configured, `docs/user/UPDATES.md` documents backup, checksum, install, and rollback steps, and release docs require the roadmap checklist to close before tagging or publishing. |
| Backup, restore, export, import, recovery, and safe support-report flows | Complete locally | Settings backup/restore now covers settings, saved searches, and cover letter templates; generated backups include a validated recovery guide that names portable coverage, excluded private/session data, and the separate full-local-recovery risk. Settings shows the same Recovery coverage copy and points users to safe support reports before broader recovery. 2026-06-19 evidence: settings backup tests `28/28`. |
| Beginner-readable install, first-run, privacy, source, and troubleshooting docs across README, docs, wiki, and release notes | Complete locally | `e7a4ba43` refreshed the public README, docs hub links, renamed feature docs and screenshots, archived stale harness reports, updated release-readiness sensors, and kept final wiki/main push held for the end of the goal. |
| Accessibility, keyboard, screen-reader, narrow-width, empty/loading/error, and platform parity checks | Complete locally | Post-docs final 2026-06-19 evidence refreshed README screenshots with `npm run docs:screenshots` 9/9 and reran `npm run test:e2e:all:budget`: 278/278 expected across Chromium and WebKit in 133838.84 ms, with zero unexpected, flaky, or skipped results. UI ledger records route/action/state coverage. |

### External AI Readiness

External AI stays optional, disabled by default, and gateway-bound. For this
goal, the user-facing provider setup cannot remain future-only; implement the
Settings UI, local-vault credential storage, privacy labels, and provider
transport boundary while keeping every outside-AI feature previewed, redacted,
editable, cancellable, and explicitly approved before anything leaves the
device.

| Requirement | State | Evidence or next action |
| ----------- | ----- | ----------------------- |
| Settings UI for providers and privacy labels | Complete locally | Settings exposes beginner-readable optional Outside AI setup for OpenAI, Anthropic, Google Gemini, GitHub Copilot, and custom HTTPS providers, with provider order, per-provider model names, review/privacy toggles, local-only default state, and plain copy. |
| Provider credentials through `CredentialService` and local vault | Complete locally | Provider credential keys, save/status UI, and backend retrieval use `CredentialService`; provider secrets are not serialized into config, backups, frontend state, prompts, logs, or request-history storage. |
| Payload preview, redaction, edit, and cancel UI | Complete locally | `ExternalAiReviewDialog` exposes review/edit/cancel before send. Gateway tests enforce preview, redaction, approval, data-category policy, and prompt-like content blocking. Job-card public posting summaries now use this dialog before provider calls. |
| Local request-log storage and viewer | Complete locally | Metadata-only request history keeps feature, provider, labels, categories, and timestamp only; the Settings viewer never stores raw prompts, resumes, notes, job text, or provider responses. |
| Feature-specific local fallback or external-AI-required label | Partial | First shipped outside-AI feature is optional public posting summary from a job card; local job viewing, scoring, notes, tracking, and resume workflows still work without outside AI. Future private-data AI features remain blocked until they expose the same data-category labels, review, and backend-validation path. |
| Provider transports only behind the gateway | Complete locally | `send_external_ai_request` owns provider HTTP calls for OpenAI Responses, Anthropic Messages, Google Gemini `generateContent`, GitHub Models-compatible chat completions, and custom public HTTPS chat-compatible endpoints. The backend rebuilds prompts from reviewed public fields, retrieves keys from `CredentialService`, blocks private/full-database categories, blocks unclassified keys and prompt-like posting content, disables redirects, validates custom endpoints through the SSRF-safe HTTPS resolver, and the harness only allows provider endpoints inside the reviewed gateway boundary. 2026-06-19 evidence: focused external-AI frontend tests `99/99`, Rust external-AI tests `11/11`, `lint:external-ai`, external-AI sensor tests, `lint:bloat`, full ESLint, and Rust Clippy. |

## Milestones

| Milestone | Release Blocker | Owner | Verification |
| --------- | --------------- | ----- | ------------ |
| Final gate run from latest commit | Yes | Local release lead | Full command set in this doc |
| Focused Browser Import and Workbench recheck | Yes | Local release lead | Targeted frontend and Rust tests plus manual proof |
| Manual whole-UI signoff | Yes | Local release lead | UI verification ledger and screenshot evidence |
| README, docs, and screenshot refresh | Yes | Local release lead | Updated public docs, fresh screenshots, docs lint, and bloat checks |
| Manual scraper/source verification | Yes | Local release lead | Source-debug ledger, fixtures, live probes, and restricted-source artifact check |
| Manual resume capability verification | Yes | Local release lead | Resume corpus runbook and evidence across every resume surface |
| Wiki and docs sync | Yes | Local release lead | Docs diff plus docs/harness checks |
| Push main, wiki, and `v2.9.0` tag | Yes | Local release lead | Hosted workflow starts from verified commit |
| Published asset verification | Yes | Local release lead | Checksums, SBOMs, artifacts, installers, skills archives |
| Signed Windows and Gatekeeper-ready macOS | No for no-account release | Credential owner | Credential-backed release proof; not required while Apple Developer credentials are unavailable |
| Broader source adapters | Yes for this active goal | Local release lead | Source-debug fixtures, taxonomy evidence, and live probes |
| Advanced browser assistance | Yes for this active goal | Local release lead | UI, privacy, and restricted-source tests |
| Full-feature product checklist | Yes for this active goal | Local release lead | Every non-external checklist row closed with evidence |

## Verification

Focused docs checks for this plan:

```bash
npm run lint:docs
npm run lint:bloat
npm run harness:check
npm run harness:score
git diff --check
```

Full release checks before tagging:

```bash
npm run harness:check
npm run release:readiness -- --version 2.9.0
npm run release:check-deps
npm run doctor
npm run lint:docs
npm run lint:bloat
npm run lint:security
npm run lint
npm run test:run
npm run test:e2e:all
npm run build
cd src-tauri && cargo fmt --all -- --check
cd src-tauri && cargo clippy -- -D warnings
cd src-tauri && cargo test --lib
```

## Progress

- Current local state: `2.9.0` release operations are paused while final-gate
  verification work is completed.
- Current product state: the active v2.9.0 product checklist is not closed.
  Final local gates, publication, hosted asset verification, and external
  signing gaps remain. Focused source, restricted browser-import, resume-corpus,
  resume UI, semantic-matching, README/docs, screenshot, and whole-UI evidence
  has been recorded locally and must be included in the final gate handoff.
- 2026-06-19 progress: release gates found dependency drift and updated
  `@tauri-apps/cli` to `2.11.3`; source recognition expanded Application
  Assist ATS detection for additional career systems already listed in the
  shared source taxonomy without adding new scraper transport or submit
  automation.
- 2026-06-19 progress: LinkedIn-compatible Workbench local activity ledger
  now covers explicit user-clicked rejected, interview, follow-up, and reminder
  events in addition to applied, saved, tracking, notes, and not interested.
  The flow still stores only local user-confirmed records and does not read
  hidden restricted-site state.
- 2026-06-19 progress: Settings backup/restore now exports and restores a
  local-data bundle for settings, saved searches, and cover letter templates,
  validates backup recovery guidance, and tells users what broader full local
  recovery can replace before they proceed.
- 2026-06-19 progress: LinkedIn-compatible Workbench now includes optional
  local learning from user-clicked Workbench actions. It starts off, stores
  only title/company/action signals, excludes browser/session data, shows
  reviewable suggestions, and lets the user clear learned signals.
- 2026-06-19 progress: local interest learning now covers explicit saved,
  dismissed, saved-search, note, Browser Import, and Useful/Not for me feedback
  actions without storing note text, URLs, browser state, cookies, or hidden page
  data.
- 2026-06-19 progress: source mining added BDJobs, promoted Naukri and Bayt to
  regional restricted-source candidates, marked Google Jobs as user-opened
  visible-result import research, and kept Scrapling/Stygian-class anti-detection
  tooling out of the v2.9.0 native source path.
- 2026-06-19 progress: script tests moved from the flat `scripts/` root into
  `scripts/tests/` and `scripts/security/tests/`; harness references and focused
  planner checks now use the new layout.
- Next best action: run final local gates from the verified commit before any
  push, tag, hosted build, or publication action.

## Decision Log

- Do not release merely because the secure foundation is verified. The active
  goal now requires the backlog completion checklist to close first.
- A premature remote `v2.9.0` tag and draft release exist from earlier release
  sequencing. Do not publish them; delete or replace them only after explicit
  user approval.
- Keep public unauthenticated sources low-friction where source behavior allows
  it.
- Keep authenticated restricted sources user-controlled, warning-gated, and
  local, without token or session storage.
- Use Browser Import and local activity ledgers as the bridge between helpful
  automation and privacy-preserving user control.
- Treat signed distribution as an upgrade path when credentials exist, not as a
  blocker to the no-account release path.

## Outcomes And Retrospective

Open until `2.9.0` release assets are published and verified.

After release, move this file to completed or split the remaining full-feature
work into versioned follow-up plans.
