# v2.9.1 Maintenance And Repo Cleanup

Last updated: 2026-06-22.

Completed status: `v2.9.1` was published on 2026-06-23 UTC from tag
`v2.9.1` at `81e2df0e`. The GitHub release is non-draft, non-prerelease, and
latest. Public assets and the no-account macOS DMG were verified after
publication, and the public wiki pages were pushed.

## Problem

`v2.9.0` shipped after a large release push. `v2.9.1` should be a small patch
line that cleans the repo, corrects stale state, and fixes confirmed
maintenance issues without expanding product scope.

## Product Direction

The product direction for this patch is maintenance and repo cleanup. Users
benefit when the repo is easier to verify, release claims are accurate, and
regressions are fixed without adding new behavior to learn.

Use these defaults:

- No new product features.
- Prefer deletion, consolidation, and standard repo tooling over new
  abstractions.
- Preserve `v2.9.0` behavior unless fixing a confirmed regression.
- Keep warning, consent, review, and local-first boundaries intact.
- The hard stops remain privacy and security failures, credential or session
  capture, hidden background access, unsafe destinations, data loss, platform
  control bypass, unbounded source access, and external side effects the user
  did not choose.

## Scope And Non-Goals

In scope:

- Active plan cleanup after `v2.9.0` publication.
- Maintained-doc drift cleanup, especially stale pre-publication wording.
- Harness, bloat, docs, security, dependency-pin, and test-quality fixes.
- Deleting disposable generated artifacts or duplicate maintained state.
- Small bug fixes with focused regression coverage.
- Final `v2.9.1` changelog, release notes, wiki sync review, and release
  verification when the cleanup checklist is closed.

Non-goals:

- v3 implementation.
- New source adapters, scraping behavior, browser automation, or restricted
  source capabilities.
- New external AI features or provider transports.
- Broad UI redesign.
- Dependency additions unless current tooling is broken and no smaller path is
  available.
- Calling the broad repo-wide quality goal complete.

## Maintenance Checklist

### 1. Active State Cleanup

- Move completed `v2.9.0` release planning out of `docs/plans/active/`.
- Keep active restart files limited to `status.md`, `current-work.md`, and this
  maintenance plan.
- Update `docs/plans/index.json`, `docs/plans/README.md`, `docs/ROADMAP.md`,
  and `docs/harness/manifest.json`.
- Defer v3 implementation from the active workstream index.

### 2. Stale Release Claims

- Scan maintained docs for `v2.9.0` pre-publication language.
- Keep release evidence ledgers when they are clearly historical snapshots.
- Update or move any maintained docs that still treat old publication, tag
  movement, or hosted asset verification work as pending.
- Keep macOS and Windows signing gaps accurate.

### 3. Repo Cleanup

- Delete disposable generated artifacts only after confirming they are not
  tracked release evidence.
- Remove duplicate maintained state when one source of truth is enough.
- Keep machine-specific absolute local paths out of tracked docs and code.
- Keep active docs under harness context budgets.

### 4. Maintenance Fixes

- Accept only confirmed bug, docs, harness, security, privacy, portability, or
  release-tooling fixes.
- Add the smallest focused check that would catch a regression.
- Do not start broad refactors or abstractions without a failing gate or a
  concrete maintenance blocker.

### 5. Release Cut

- Package, Tauri, and Cargo metadata already target `2.9.1`; update changelog
  and release notes only when the patch release is ready to cut.
- Run `npm run release:readiness -- --version 2.9.1` and the focused matrix
  checks for touched surfaces.
- Verify public assets only after the release is published.

## Acceptance Criteria

- Active restart docs and plan index no longer route agents into `v2.9.0`
  publication work.
- v3 implementation is deferred during the patch line.
- Package, Tauri, and Cargo metadata target `2.9.1`; the public latest release
  is now `v2.9.1`.
- Every cleanup change is backed by focused docs, harness, or regression
  evidence.
- Cleanup-release regression validation has current local evidence and explicit
  unclaimed gaps in
  [`docs/harness/full-manual-validation-v2.9.1.md`](../../harness/full-manual-validation-v2.9.1.md)
  before any release-ready claim.
- Rule 0 is unchanged.

## Verification

For plan/docs cleanup:

```bash
npm run harness:plan -- --since origin/main
npm run harness:check
npm run lint:docs
npm run lint:bloat
npm run harness:score
git diff --check
```

Broaden only when code, release workflows, security, privacy, packaging, or UI
behavior changes.

## Progress

- 2026-06-22: Created maintenance-only patch plan. Live evidence showed
  `v2.9.0` published on GitHub while active docs still described publication as
  active. The first cleanup slice moves v2.9.0 planning to completed history and
  replaces active routing with v2.9.1 maintenance scope.
- 2026-06-22: Completed active-state cleanup. Moved the v2.9.0 roadmap to
  `docs/plans/completed/`, updated active status, current work, plan index,
  docs index, roadmap, docs hub, harness manifest, and v2.9.0 risk-register
  status, deferred v3 implementation, and removed ignored local `docs/.DS_Store`
  clutter. Verification passed: `npm run harness:plan -- --since origin/main`,
  `git diff --check`, `npm run harness:check`, `npm run lint:bloat`,
  `npm run lint:docs`, `npm run harness:score`,
  `npm run test:scripts -- scripts/tests/check-harness-policy.test.mjs scripts/tests/harness-score.test.mjs scripts/tests/harness-session.test.mjs`,
  and `npm run harness:session -- --json`.
- 2026-06-22: Fixed the `harness:session` script-test count after script tests
  moved under `scripts/tests/` and `scripts/security/tests/`. Maintained
  v2.9.0 capability and resume-corpus ledgers now read as completed release
  evidence instead of pre-publication handoff state.
- 2026-06-22: Updated the maintained release process to require closing the
  current active release or maintenance checklist instead of the completed
  v2.9.0 release backlog before publication.
- 2026-06-22: Split the near-cap root `CHANGELOG.md` into current release
  history plus under-cap release-band archives, indexed those archives, and
  removed the frozen changelog exception from the file-size contract.
- 2026-06-22: Bumped local package, Tauri, and Cargo metadata to `2.9.1`,
  refreshed stable dependency pins, and passed version, dependency,
  harness, bloat, lint, frontend, build, Clippy, and Rust library checks.
- 2026-06-22: Replaced custom lazy statics with standard lazy initialization
  where safe and verified with lint, build, Clippy, and Rust library tests.
- 2026-06-22: Split oversized and near-budget maintenance files:
  `scripts/tests/check-security-sensors.test.mjs`, `src/mocks/handlers.ts`,
  `src/pages/Settings.test.tsx`, and the production health source-check module
  formerly named `smoke_tests.rs`. Also split the core architecture inventory
  into `docs/developer/ARCHITECTURE_CORE.md`, narrowed safe Clippy allowances,
  and refreshed the release-readiness test expectation to `2.9.1`. Verification
  passed: `npm run lint`, `npm run test:scripts`, `npm run test:run`,
  `npm run build`, `npm run lint:docs`, `npm run lint:bloat`,
  `npm run harness:check`, `npm run harness:score`,
  `npm run release:check-version -- 2.9.1`,
  `npm run release:readiness -- --version 2.9.1`,
  `cargo fmt --all -- --check`, `cargo clippy -- -D warnings`,
  `cargo test --lib`, and `git diff --check`.
- 2026-06-22: Completed the current-doc stale-release-claim sweep for
  maintained release, capability, update, macOS, external-AI, and active-plan
  docs. Added
  [`docs/harness/full-manual-validation-v2.9.1.md`](../../harness/full-manual-validation-v2.9.1.md)
  as the pending live/manual validation ledger for every major surface.
- 2026-06-22: Reframed the validation ledger around cleanup-release regression
  evidence, recorded local browser, frontend, backend, build, docs, bloat,
  harness, and isolated macOS native startup evidence, and kept Windows, Linux,
  public assets, live restricted-source sessions, and credentialed external
  sends unclaimed.
- 2026-06-22: Added local v2.9.1 release notes and changelog entries for the
  maintenance release cut.
- 2026-06-22: Final local repo pass found a startup keyboard shortcut timing
  race in the full E2E budget run. Replaced the shortcut provider's post-paint
  startup effects with layout effects so app-wide shortcuts are registered
  before the visible shell is interactive. Verification passed with focused
  Chromium/WebKit keyboard E2E, full frontend tests, production build, and the
  full E2E budget.
- 2026-06-22: Prepared public-wiki page updates in the sibling wiki
  checkout for `Home.md` and `Capabilities.md`; after publication, pushed the
  wiki pages at `78f9b2b`.

## Decisions

- Package metadata targeted `2.9.1` before publication; the publication slice
  completed and public `v2.9.1` assets are now verified.
- Do not implement v3 foundation work during the v2.9.1 patch line.
- Treat release ledgers as historical evidence unless they are linked from an
  active restart surface as current work.

## Outcomes And Retrospective

Completed. `v2.9.1` shipped as the intended maintenance and repo cleanup
release without adding product scope. The release preserved `v2.9.0`
user-facing behavior, refreshed package and crate pins, reduced oversized
maintenance surfaces, narrowed stale release claims, added a cleanup regression
ledger, and fixed the startup keyboard shortcut timing race found during final
E2E validation.

Release evidence:

- Local `main`, `origin/main`, and tag `v2.9.1` point at `81e2df0e`.
- Release workflow run `27990965207` passed and published
  `JobSentinel 2.9.1` at `2026-06-23T00:06:36Z`.
- `npm run release:verify:public -- --tag v2.9.1 --platforms windows,macos,linux`
  passed against public release assets.
- `npm run tauri:verify:macos:latest -- --tag v2.9.1` passed against the
  public no-account universal DMG. Gatekeeper rejection remains expected for
  the no-account path.
- Public wiki pages `Home.md` and `Capabilities.md` were pushed from the
  sibling wiki checkout at `78f9b2b`.

Retrospective:

- The release was correct but too slow. Hosted publication took about
  40 minutes, and the hosted macOS leg alone took 19m18s.
- Future no-account releases should prefer a locally built and locally verified
  macOS DMG, with hosted macOS retained for explicit full cross-platform proof
  or Developer ID/Gatekeeper releases.
- Public release verification must be explicit. The separate
  `Verify Release Artifacts` workflow did not appear in the latest GitHub run
  list for the workflow-token-published `v2.9.1` release, so release docs and
  workflow behavior must not assume that release-event verification happened.

Follow-on active work is tracked in
[`release-pipeline-audit-and-optimization-deferred-2026-07-13.md`](../archive/release-pipeline-audit-and-optimization-deferred-2026-07-13.md).
