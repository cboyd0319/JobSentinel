# Technical Debt Tracker

Use this file for durable debt that affects agent reliability, architecture,
security, or developer workflow.

## Open Items

| ID | Area | Evidence | Risk | Next step | Status |
| -- | ---- | -------- | ---- | --------- | ------ |
| HE-009 | Sensor modularity | `scripts/check-repo-bloat.mjs` is still over 4,500 lines and mixes stale docs, most privacy logging, fixture drift, broad-audience checks, technical-first copy checks, and source security patterns. Filesystem, tracked-artifact, dependency-ownership, source-structure, E2E-helper, product-framing, product-copy, release-promise, initial privacy-logging, and IPC-minimization checks now live under `scripts/harness/checks/` with focused tests. | Large mixed sensors are hard to review and extend safely. | Continue moving named check families under `scripts/harness/checks/`, starting with docs drift, remaining privacy logging, fixture quality, broad-audience checks, technical-first copy checks, and source security patterns. | In progress |
| AUD-001 | Broad-audience fixture drift | Broad-audience audit on 2026-05-31 found company suggestions, mocks, E2E examples, config comments, source defaults, source recommendations, synonym groups, and backend fixtures still skewed toward software/tech roles. The latest slices rebalanced company suggestions, visible placeholders, E2E examples, frontend/mock ATS fixtures, generic market-intelligence, notification, config, database, ATS, ghost, salary, scoring, generic scraper adapter, command, mock job data, salary-normalization, sample JSON Resume, company fallback, bookmarklet, import, dashboard search, score-modal fixtures, tech-heavy source defaults/recommendations, and synonym coverage that were not explicit software-engineer/SWE branch coverage; focused bloat coverage now rejects software-only drift in the generic market-intelligence, generic scraper, salary-normalization, code-profile, sample-resume, bookmarklet, search-history, and score-modal fixture paths. | Default examples and source suggestions shape future product decisions and can make JobSentinel feel engineer-first even though the product is for technical and non-technical job seekers. | Keep tech-specific cases only when they test a tech-specific source, normalization rule, source-realism parser, or taxonomy branch. Continue broad-audience fixture audits in less obvious paths, then tighten sensors where allowlists are stable. | In progress |
| UX-002 | Zero-technical UX audit | UX audit on 2026-05-31 found settings failure, application empty state, no-jobs recovery, browser import setup, email setup, install docs, location copy, missing-detail import dead ends, hidden Prepare Form access, stale Application Assist recovery paths, stale database restore guidance, silent invalid settings restore, Schema.org/import jargon, webhook protocol/port jargon, advanced raw problem export framing, diagnostic-first source health, and Cloudflare-protection warnings. The latest slices fixed first-use and visible-copy issues, allowed saving missing-detail import previews, exposed Prepare Form on dashboard job cards, added Set Up Profile recovery, blocked stale `Settings > Application Assist` copy, softened optional email/USAJobs setup, changed screening-answer setup to plain question wording, replaced stale database recovery copy, separated cancelled restore from unreadable backup files, made connection-link validation provider-first, reframed raw export as advanced support, changed job-import errors to browser-address-bar guidance, added source-health next-step guidance, and replaced advanced source-protection jargon with search-link/browser-link recovery. Full zero-technical review across all settings panels and advanced setup paths remains incomplete. | Users with no technical background can still abandon setup if later panels expose protocols, ports, source-specific jargon, or dead-end states. | Continue UI walkthroughs for notification setup, import setup, source setup, empty states, and error states. Prefer provider-first flows, plain recovery copy, and account-optional support paths. | In progress |
| SEC-002 | Rust informational advisory watch | `cargo audit` on 2026-05-29 reports 0 vulnerabilities and 19 informational warnings: GTK3/glib advisories through Tauri's Linux stack, `unic-*` through Tauri `urlpattern`, `paste` and `number_prefix` through ML/tokenizer dependencies, and `proc-macro-error` through proc-macro dependencies. | These are not current direct vulnerability findings, but stale upstream transitive dependencies can become real security or maintenance risk if ignored. | Keep Tauri, wry, tao, tokenizers, candle, and related crates on latest stable; rerun `cargo audit` and `cargo deny check advisories` after dependency changes; remove ignores as soon as upstream paths disappear. | Open |
| BLOAT-003 | Removing bloat and junk | User reported root clutter and likely nested junk after the disposable-artifact sensor was already passing. | Artifact-only sensors can miss stale docs, redundant root files, duplicate examples, or obsolete nested content that still slows maintenance. | Classify root files, sweep nested paths for stale or duplicate content, remove or relocate confirmed bloat, then add sensors for recurring junk classes. | Open |

## Closed Items

| ID | Area | Evidence | Outcome | Closed |
| -- | ---- | -------- | ------- | ------ |
| SEC-007 | Resume file path exposure | Security audit on 2026-05-31 found Application Assist stored `resume_file_path` in SQLite and returned/displayed the raw path through profile IPC and UI. | Kept the local path available only to backend attach logic, changed profile IPC/UI to expose only `hasResumeFile` plus `resumeFileName`, preserved saved paths on unrelated edits through explicit replace/clear semantics, and added IPC-minimization coverage for resume-path DTO drift. | 2026-05-31 |
| SEC-006 | Bookmarklet import token IPC | Security audit on 2026-05-31 found `get_bookmarklet_config` returned the raw bookmarklet auth token to the renderer, and the frontend embedded it in copied browser-button code. | Removed the token from the bookmarklet config DTO and mocks, added a backend `copy_bookmarklet_code` command that writes the browser-button code to the OS clipboard without exposing the token to React state, and added an IPC-minimization sensor for token DTO drift. | 2026-05-31 |
| SEC-008 | Support-report sanitizer coverage | Security audit on 2026-05-31 found Rust support-report sanitization did not redact generic URLs or common `access_token`, `refresh_token`, `password`, and bookmarklet token forms before copied or saved reports could be shared. | Hardened the Rust sanitizer for generic URLs, query secrets, password-like values, and `X-JobSentinel-Token` fields, with focused sanitizer tests. | 2026-05-31 |
| SEC-005 | IPC payload minimization | Read-only security audit on 2026-05-31 found non-settings screens could request full application profile payloads, import URLs preserved raw pasted data, and Dashboard read full config for two fields. | Added `has_application_profile`, `get_application_profile_preview`, `get_dashboard_preferences`, canonical import URL minimization before preview/hash/storage, `{ jobId }` import responses, focused tests, and harness sensors for wrapper invokes and import mock return shape. | 2026-05-31 |
| UX-001 | Zero-technical support and recovery | Read-only UX audit on 2026-05-31 found `SubmitOptions` and Quick Start still center GitHub/Drive issue paths, settings crash recovery lacked a safe report action, problem-history/export labels exposed technical details, setup docs mentioned notification providers beyond the wizard, and generated reports used system/config/structured-data language. | Made local safe report saving the recommended feedback submit path, kept GitHub optional, added safe-report copy/save actions to modal crash recovery, aligned Quick Start setup wording, simplified problem-history/export labels, made saved-report success steps account-optional, and reworded generated reports with plain support labels. | 2026-05-31 |
| SEC-004 | Privacy logging audit slice | Read-only security audit on 2026-05-31 found raw automation dropdown values in Rust tracing and raw frontend `console.error` forwarding/default browser logging. | Removed raw selected values and selectors from automation dropdown logs, sanitized forwarded console arguments, suppressed raw global default console output after capture, and added bloat plus Vitest regression coverage. | 2026-05-31 |
| HE-011 | Active plan compaction | Active plans contained long historical progress tables and the handoff could lag current local commit state. | Added `docs/plans/active/status.md` as the compact restart surface, archived older progress rows in `docs/plans/archive/progress-history-2026-05-28-to-2026-05-29.md`, and routed the plan index, harness guide, agent guide, and handoff toward the compact state while keeping detailed plans authoritative. | 2026-05-31 |
| HE-008 | Harness policy manifest | Deep harness audit on 2026-05-31 found `scripts/check-harness.mjs` hardcoded required files, snippets, and README external-reference policy. | Added `docs/harness/manifest.json` for required harness files, required policy snippets, and README reference-source coverage; `scripts/check-harness.mjs` now loads the manifest and focused script tests guard against large policy tables drifting back into the checker. | 2026-05-31 |
| HE-010 | Environment doctor platform coverage | Deep harness audit on 2026-05-31 found `npm run doctor` did not check Linux Tauri system packages, Playwright browser launch readiness, or local toolchain drift from CI baselines. | Added Linux `pkg-config` checks for WebKitGTK, GTK, appindicator, and librsvg plus `patchelf`; added Playwright Chromium launch readiness; added `npm run doctor:e2e` as a strict E2E gate; and added Node/Rust CI-baseline drift warnings with script coverage. | 2026-05-31 |
| SEC-003 | External AI provider surface | Deep harness audit on 2026-05-31 found provider detection was narrow and mostly code-only. | Added `npm run lint:external-ai` and wired it into `npm run harness:check`; the sensor scans code plus JSON, YAML, TOML, and env-style config for external AI endpoints, SDK imports, dependency declarations, hosted inference endpoints, and provider API-key variables outside `src/services/aiGateway.ts`. | 2026-05-31 |
| HE-007 | CI harness coverage | Deep harness audit on 2026-05-31 found normal CI lacked `npm run harness:check` and `npm run test:scripts`, while docs-harness path filters watched only part of the sensor script set. | Added a dedicated normal-CI harness job for `npm run harness:check` and `npm run test:scripts`; widened Docs Harness path filters to `scripts/**`; and added script tests to Docs Harness. | 2026-05-31 |
| REL-001 | Release preflight | Tag and manual build workflows built and uploaded artifacts without first running repo harness checks or version/tag consistency checks. | Added release-version validation across `package.json`, Tauri config, and Cargo metadata; gated tag releases with harness, script-test, markdown, frontend-build, Rust fmt, clippy, and unit-test preflight; added the same core preflight to manual Linux and Windows build workflows before artifact upload. | 2026-05-31 |
| SEC-001 | Rust transitive dependency alert | Dependabot alert #67 for `rand` 0.7.3 was accepted temporarily on 2026-05-28 because it entered only through the upstream Tauri build-time chain. The latest-stable dependency refresh removed the `rand` 0.7 and `rsa` advisory paths from `Cargo.lock`; `cargo audit` exits 0 with only allowed warnings. | Closed the exception by upgrading the Rust dependency graph to current stable Tauri and related crates instead of carrying the dismissed alert forward. | 2026-05-28 |
| BLOAT-001 | Removing bloat and junk | Root and nested paths needed repeat manual sweeps for disposable artifacts, while earlier tracked-file audits had already found generated output outside root. | Added `npm run lint:bloat` and wired it into `npm run harness:check` so disposable local artifacts, tracked generated output, logs, temp files, database files, installer packages, and AI-generated root summary files fail deterministically; current repo scan passes with only expected local caches ignored. | 2026-05-20 |
| SCRAPE-001 | Scraper retry coverage | Several source adapters called `reqwest` directly for custom headers, query parameters, JSON bodies, or API auth clients instead of shared retry helpers. | Added shared custom-request retry helpers, routed source adapters through them, and preserved source-specific response handling for anti-bot, auth, and schema fallback cases. | 2026-05-19 |
| BLOAT-002 | Duplicate docs screenshot artifacts | `tests/e2e/docs/images/` contained tracked copies of documentation screenshots while `README.md` and screenshot tooling use canonical `docs/images/`. | Removed the stale tracked screenshot bundle and ignored `/tests/e2e/docs/` so future Playwright artifacts do not re-enter the repo. | 2026-05-19 |
| QA-005 | Testing docs drift | Active testing docs still demonstrated runtime Playwright skips and focused-test source modifiers after skip-sprawl cleanup. | Replaced those examples with command-line focused-run guidance and added bloat coverage so maintained testing docs cannot reintroduce runtime skip or focused-test modifier patterns. | 2026-05-20 |
| QA-004 | E2E skip sprawl | Playwright suites contained many runtime `test.skip()` branches that allowed stale page objects and removed UI flows to pass without exercising behavior. | Replaced skip-heavy suites with current mock-backed assertions across job interactions, filtering, application tracking, settings, resume, market intelligence, Application Assist, resume builder, and keyboard navigation; `tests/e2e/playwright` now has no `test.skip()` or `.only` matches. | 2026-05-20 |
| SCRAPE-002 | Rate limiter contention | `RateLimiter::wait` held the shared bucket mutex while sleeping for token refill. | Released the lock before sleeping and added regression coverage so one exhausted bucket cannot block unrelated scrapers. | 2026-05-19 |
| HE-004 | Tauri invoke map | `scripts/check-tauri-invokes.mjs` verifies production frontend `invoke()` command names against `src-tauri/src/main.rs` registrations through `npm run harness:check`. | Frontend/backend IPC drift now fails locally before broken workflows ship. | 2026-05-19 |
| HE-005 | Tracked generated output | `src-tauri/coverage/` was committed despite generated coverage output being ignored. | Removed tracked coverage artifacts from repo history tip; future coverage output remains ignored by `.gitignore`. | 2026-05-19 |
| HE-006 | Stale migration docs | Unlinked migration and summary docs duplicated authoritative references and contained hardcoded implementation/test claims. | Removed stale docs, narrowed root-only generated-doc ignore rules, and replaced the undo note with a compact completed plan. | 2026-05-19 |
| QA-003 | Test quality sensors | `scripts/check-test-quality.mjs` rejects no-op `expect(true)`, always-true fallbacks, focused `.only` tests, and skipped unit tests through `npm run harness:check`. | Repeated weak assertion drift and hidden skipped component tests now have a mechanical guard. | 2026-05-19 |
| HE-003 | Security sensors | `scripts/check-security-sensors.mjs` verifies required security docs, security matrix coverage, and CI audit/advisory gate visibility through `npm run harness:check`. | Local harness now summarizes security-specific gates so agents get a deterministic threat-surface reminder. | 2026-05-19 |
| HE-001 | Harness docs | `scripts/check-harness.mjs` rejects hardcoded current test-count claims in front-door and testing docs. | Maintained docs now reference fresh command output for current test counts instead of stale numeric claims. | 2026-05-19 |
| HE-002 | Architecture sensors | `scripts/check-frontend-boundaries.mjs` enforces production frontend import boundaries and runs through `npm run harness:check`. | Shared frontend layers now have a mechanical drift sensor. | 2026-05-19 |
| QA-002 | E2E assertion quality | Search for always-true expressions such as `expect(true)` and `\|\| true` no longer finds matches under `tests/e2e/playwright`. | Replaced no-op assertions with real UI assertions; later QA-004 removed the runtime skip branches that remained after this first cleanup. | 2026-05-19 |
| QA-001 | Rust test linting | All-target clippy with warnings-as-errors currently reports test-target lint debt, while production clippy passes. | Added explicit policy: production clippy is the hard gate, test-target clippy warnings are advisory until test lint policy tightens. | 2026-05-19 |
