# Harness Sensor Registry

This is the canonical lifecycle and decision record for repeated harness
feedback. Product checks may have deeper implementation owners, but each
retained control records the protected risk, outcome evidence, operator cost,
platform boundary, and retirement trigger here.

| ID | Purpose and protected risk | Command and implementation | Area | Triggers | Before and after evidence | Signal, false-positive cost, and response | Platforms | Owner | Review or retire when |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| harness-contract | Reject contradictory owners, state, workflow policy, or limits | `npm run harness:check`; `scripts/harness/contract.mjs` | all five subsystems | harness, state, workflow, or policy change | Pre-standard duplicate owners in the dated audit; current focused contract fixtures and live pass | Nonzero names the owner; a false positive blocks all work, so prove it with an isolated valid fixture before policy changes | shared Node core; native-host evidence recorded separately | repository maintainers | review on owner or schema change; retire if a standard tool enforces identical semantics |
| file-size | Prevent unreviewed monolith growth and permanent waivers | `npm run lint:file-size`; streaming bounded scanner | architecture | source or configuration change | Pre-canonical oversized baseline; current line, byte, CRLF, exclusion, symlink, and ratchet fixtures | Hard violations fail and review candidates list; noise costs review time, so correct classification or an exact ratchet, never the canonical threshold | shared Node core | architecture owner | review on new file class; retire when native tools enforce identical semantics |
| architecture | Prevent unowned roots, graph drift, deep coupling, and nonportable paths | `npm run lint:architecture`; structured policy, TypeScript, and Cargo graph checks | architecture | source, manifest, project, or topology change | Legacy graph in the active blueprint; current valid and hostile root, edge, cycle, config-project, C#, collision, and junction fixtures | Nonzero names the boundary; false positives block a unit, so compare the structured owner and fixture before changing policy | shared parsers and Cargo metadata | architecture owner | review on unit or ecosystem change; retire when native graphs enforce every boundary |
| verification-router | Prevent changed surfaces from bypassing required feedback | `npm run harness:plan -- --since <ref>`; shared path classifier | feedback | any diff | Three missing lanes in `harness-canonical-remediation-effectiveness-2026-07-15.json`; current held-constant success and routing fixtures | Prints commands, reasons, and paths; over-routing costs time, so narrow only with a hostile single-path fixture | shared Node core | repository maintainers | review on owner or command change; retire when equivalent local routing replaces it |
| state | Prevent rediscovery, concurrent objectives, and unsupported passing claims | `npm run harness:session`; root state validator | state | session start, transition, blocker change, session end | Historical append-only evidence archived; current contradiction, blocker, passing-evidence, and fresh-session fixtures | Invalid state fails; false positives stop resumption, so repair canonical state or a parser fixture rather than bypassing | shared Node core | repository maintainers | review on state schema change; retire only if work becomes provably single-session |
| environment | Prevent mutation before prerequisites and divergent clean or warm setup | `./init.sh`, `init.ps1`, `npm run doctor`; shared Node initializer | environment | init, runtime, dependency, or platform change | Earlier install-first path; current preflight, clean/warm, offline, missing-prerequisite, spaces, and Windows-command fixtures | Doctor distinguishes pass, warning, and failure; false failures block setup, so retain exact remediation and reproduce in an isolated root | Windows, macOS, Linux; native run gap stays explicit | repository maintainers | review on runtime or platform change; retire if a hermetic supported environment replaces host diagnosis |
| script-contracts | Prevent repository-owned semantic tooling from drifting silently | `npm run test:scripts`; `scripts/tests/` | tools and feedback | repository script change | Dated evidence records prior monolithic checks; current focused fixtures cover each retained semantic core | Fixture failure is exact; suite noise costs developer time, so reduce to the focused test before policy edits | shared Node core | repository maintainers | review when a script lacks a hostile fixture; retire with the owning script |
| privacy-security | Prevent credential, local-data, workflow, and egress regressions | `npm run lint:security`; security sensors and secret scanner | security | privacy, credential, dependency, hook, or workflow change | Archived security checkpoints; current hostile fixtures, release-workflow routing, and live sensor pass | Exact boundary violation; false positives can block release, so identify the protected risk and add a safe fixture before changing a sensor | shared Node core | security owner | review on trust-boundary change; retire only when equivalent mandatory scanning supersedes it |
| frontend | Prevent type-graph, lint, and behavior regressions | `npm run typecheck && npm run lint && npm run test:run`; composite TypeScript build | web unit | frontend or TypeScript configuration | Omitted Playwright config failed the first composite build; current project-graph fixture and `tsc --build` pass | Compiler, lint, or test failure; false positives block the web unit, so correct the owning config or focused test | Node and browser contracts | frontend owner | review on project or runtime split; retain while the frontend exists |
| rust | Prevent workspace, ownership, lint, and behavior drift | `npm run verify:rust`; Cargo metadata and workspace tools | Rust units | Rust source, workspace, SQLx, or Tauri change | Legacy two-crate baseline and archived full runs; current topology fixtures and pending fresh full run | Format, Clippy, or test failure; false positives are costly, so reproduce by package and preserve native-host gaps | Rust toolchain and native targets | Rust owners | review on crate-edge change; retain while Rust exists |
| runtime-smoke | Prevent structurally passing changes from breaking the restartable golden journey | `npm run test:e2e:smoke:budget`; Playwright bounded journey | runtime feedback | cross-component behavior or completion | Archived runtime checkpoints; current run must record reset, outcome, timing, and first material cause | Journey, timeout, or budget failure; reruns cost minutes, so reset once and classify infrastructure separately | Chromium contract; native desktop separate | product and reliability owners | review on golden-journey change; retire only for a faster equivalent journey |
| pre-alpha-private-no-ci | Keep the explicit no-CI scope visible and prevent automatic CI from returning silently | `npm run harness:check`; `harness-manifest.json` exception and workflow-trigger scan | feedback governance | manifest or hosted workflow change | User directive on 2026-07-14; current exception fixture and absent retired CI path | Any automatic CI trigger or incomplete exception fails; the cost is no shared-history backstop, so retire before public alpha, external contributors, or protected-history reliance | shared Node core; native Windows evidence remains a gap | repository owner | retire at the first recorded review trigger and restore authoritative CI |
| pre-commit | Catch secrets and staged defects before commit without making hooks authoritative | `.husky/pre-commit`; `scripts/harness/pre-commit.mjs` | local feedback | explicitly installed local commit | Earlier direct untested commands; current passing and failing status-preservation fixtures | Nonzero preserves status; local false positives interrupt commits, so `HUSKY=0` or `--no-verify` is recovery only and the applicable local gate must run afterward | shared Node core through Husky on Windows and macOS | repository maintainers | review on staged policy change; retire for equivalent lower-friction local feedback |
