# Vale configuration starter
# Docs: https://vale.sh/docs/
StylesPath = Styles
MinAlertLevel = suggestion

# Only lint these file types
[*.{md,mdx,txt}]
BasedOnStyles = TechDocs, Spelling

# Optional: ignore directories
# [*.md]
# BlockIgnores = (node_modules|dist|build|.git)
